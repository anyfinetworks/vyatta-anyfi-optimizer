type: u32
help: Fixed number of ports per SDWN service
val_help: u32:1-65535; Number of TCP/UDP ports
syntax:expression: $VAR(@) >= 1 && $VAR(@) <= 65535; \
       "Number of ports must be between 1 and 65535."
